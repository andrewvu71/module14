{{> header}}
<main>
  {{#each posts}}
  <article>
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <p>By {{this.user.username}} on {{this.createdAt}}</p>
  </article>
  {{/each}}
</main>
{{> footer}}
